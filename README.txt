Creates an xls file from two files.

One file has a list of sentences where each sentence is on a line

The other file has columns where each line in the column corresponds to a word associated with a category from its column.